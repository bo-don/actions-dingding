{
  "name": "actions-dingding",
  "version": "1.0.0",
  "description": "GitHub Action - 钉钉机器人通知",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --minify",
    "test": "node index.js"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
